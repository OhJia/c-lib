import React from "react";
// We want to always get from source
import { Button } from "../lib/c-lib-button";

export default { title: "Button" };

export const primary = () => <Button>Buttno</Button>;
